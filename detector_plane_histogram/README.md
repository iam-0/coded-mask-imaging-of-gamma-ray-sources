This file contains the detector_plane_histogram (DPH) without noise.
The noise is added randomly to each element in the array while treating it as input to recreate image using any of the algorithms listed. 
